// import { fromJS } from 'immutable';
// import { makeSelectToDoDomain } from '../selectors';

// const selector = makeSelectToDoDomain();

describe('makeSelectToDoDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
